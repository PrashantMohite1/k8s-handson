// Fallback for local (non-Docker) testing. Inside the container this file is
// regenerated from nginx-templates/config.js.template using the APP_VERSION
// and APP_THEME environment variables, so it always reflects what was deployed.
window.APP_CONFIG = {
  version: "1.0.0",
  theme: "default"
};
